import { expect } from "chai";

describe('test', () => {
    it('should work', () => {
        expect(true).to.be.true;
    });
    it('should work', () => {
        expect(true).to.be.true;
    });
    it('should work', () => {
        expect(true).to.be.true;
    });
    it('should work', () => {
        expect(true).to.be.true;
    });
    it('should work', () => {
        expect(true).to.be.true;
    });
});